<Page
    x:Class="Store_Online.Modules.Garage.Views.CustomerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Customer">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===================================================== -->
        <!-- Toolbar -->
        <!-- ===================================================== -->

        <StackPanel
            Orientation="Horizontal"
            Margin="0,0,0,10">

            <Button
                x:Name="btnNew"
                Width="140"
                Height="40"
                Margin="0,0,10,0"
                Content="New Customer"
                Click="BtnNewCustomer_Click"/>

            <Button
                x:Name="btnRefresh"
                Width="120"
                Height="40"
                Margin="0,0,10,0"
                Content="Refresh"
                Click="BtnRefresh_Click"/>

            <Button
                x:Name="btnEdit"
                Width="120"
                Height="40"
                Margin="0,0,10,0"
                Content="Edit"
                Click="BtnEdit_Click"/>

            <Button
                x:Name="btnDelete"
                Width="120"
                Height="40"
                Content="Delete"
                Click="BtnDelete_Click"/>

        </StackPanel>

        <!-- ===================================================== -->
        <!-- Search -->
        <!-- ===================================================== -->

        <DockPanel
            Grid.Row="1"
            Margin="0,0,0,10">

            <TextBlock
                VerticalAlignment="Center"
                FontWeight="SemiBold"
                Text="Search :" />

            <TextBox
                x:Name="txtSearch"
                Width="260"
                Height="32"
                Margin="10,0,0,0"
                VerticalContentAlignment="Center"/>

        </DockPanel>

        <!-- ===================================================== -->
        <!-- Customer List -->
        <!-- ===================================================== -->

        <DataGrid
            x:Name="dgCustomer"
            Grid.Row="2"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            IsReadOnly="True"
            SelectionMode="Single"
            SelectionUnit="FullRow"
            GridLinesVisibility="Horizontal"
            AlternationCount="2"
            EnableRowVirtualization="True"
            EnableColumnVirtualization="True"
            HeadersVisibility="Column"
            MouseDoubleClick="dgCustomer_MouseDoubleClick"
            ItemsSource="{Binding Customers}"
            SelectedItem="{Binding SelectedCustomer}">

            <DataGrid.Columns>

                <DataGridTextColumn
                    Width="80"
                    MinWidth="60"
                    Header="ID"
                    Binding="{Binding CustomerId}" />

                <DataGridTextColumn
                    Width="120"
                    MinWidth="100"
                    Header="Code"
                    Binding="{Binding Code}" />

                <DataGridTextColumn
                    Width="*"
                    MinWidth="220"
                    Header="Customer Name"
                    Binding="{Binding Name}" />

                <DataGridTextColumn
                    Width="150"
                    MinWidth="120"
                    Header="Phone"
                    Binding="{Binding Phone}" />

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Page>