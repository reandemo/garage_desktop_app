<UserControl
    x:Class="Store_Online.Core.Controls.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d">

    <Grid Margin="0,0,0,20">

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="*" />

            <ColumnDefinition Width="Auto" />

        </Grid.ColumnDefinitions>

        <!--  Left  -->

        <StackPanel VerticalAlignment="Center">

            <TextBlock
                FontSize="28"
                FontWeight="SemiBold"
                Foreground="#1F2937"
                Text="{Binding Title, ElementName=Root}" />

            <TextBlock
                Margin="0,6,0,0"
                FontSize="14"
                Foreground="#6B7280"
                Text="{Binding Description, ElementName=Root}"
                TextWrapping="Wrap" />

        </StackPanel>

        <!--  Right  -->

        <Button
            Grid.Column="1"
            Width="170"
            Height="42"
            Padding="16,0"
            VerticalAlignment="Center"
            Background="{Binding ButtonBrush, ElementName=Root}"
            BorderThickness="0"
            Command="{Binding Command, ElementName=Root}"
            Cursor="Hand"
            FontSize="14"
            FontWeight="SemiBold"
            Foreground="White">

            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                <materialDesign:PackIcon
                    Width="18"
                    Height="18"
                    Margin="0,0,8,0"
                    Kind="{Binding ButtonIcon, ElementName=Root}" />

                <TextBlock VerticalAlignment="Center" Text="{Binding ButtonText, ElementName=Root}" />

            </StackPanel>

        </Button>

    </Grid>

</UserControl>