<UserControl
    x:Class="Store_Online.Core.Controls.UserProfileMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    Height="44"
    mc:Ignorable="d">

    <Grid>


        <ToggleButton
            x:Name="ProfileButton"
            Width="200"
            Height="40"
            Background="Transparent"
            BorderThickness="0"
            Cursor="Hand"
            FocusVisualStyle="{x:Null}">

            <ToggleButton.Template>

                <ControlTemplate TargetType="ToggleButton">

                    <Border
                        x:Name="ProfileBackground"
                        Padding="8,4"
                        Background="Transparent"
                        CornerRadius="8">

                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  Avatar  -->

                            <Border
                                Width="28"
                                Height="28"
                                VerticalAlignment="Center"
                                Background="#2563EB"
                                CornerRadius="14">

                                <Image
                                    Width="28"
                                    Height="28"
                                    Source="{Binding AvatarImage, ElementName=Root}"
                                    Stretch="UniformToFill" />

                            </Border>

                            <!--  User  -->

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">

                                <TextBlock
                                    FontSize="13"
                                    FontWeight="SemiBold"
                                    Foreground="White"
                                    Text="{Binding UserName, ElementName=Root}"
                                    TextTrimming="CharacterEllipsis" />

                                <TextBlock
                                    FontSize="10"
                                    Foreground="#9CA3AF"
                                    Text="{Binding UserRole, ElementName=Root}"
                                    TextTrimming="CharacterEllipsis" />

                            </StackPanel>

                            <!--  Arrow  -->

                            <materialDesign:PackIcon
                                Grid.Column="3"
                                Width="16"
                                Height="16"
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Foreground="#9CA3AF"
                                Kind="ChevronDown" />

                        </Grid>

                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">

                            <Setter TargetName="ProfileBackground" Property="Background" Value="#374151" />

                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">

                            <Setter TargetName="ProfileBackground" Property="Background" Value="#374151" />

                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </ToggleButton.Template>

        </ToggleButton>


        <Popup
            AllowsTransparency="True"
            IsOpen="{Binding IsChecked, ElementName=ProfileButton}"
            Placement="Bottom"
            PlacementTarget="{Binding ElementName=ProfileButton}"
            StaysOpen="False">

            <Border
                Width="260"
                Margin="0,8,0,0"
                Background="White"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                CornerRadius="10">

                <StackPanel>

                    <!--  Header  -->

                    <Border
                        Padding="16"
                        Background="#F9FAFB"
                        CornerRadius="10,10,0,0">

                        <StackPanel>

                            <TextBlock
                                x:Name="txtPopupUserName"
                                FontSize="15"
                                FontWeight="SemiBold"
                                Text="Administrator" />

                            <TextBlock
                                x:Name="txtPopupEmail"
                                FontSize="12"
                                Foreground="#6B7280"
                                Text="administrator@storeonline.local" />

                            <TextBlock
                                x:Name="txtPopupBranch"
                                Margin="0,4,0,0"
                                FontSize="11"
                                Foreground="#9CA3AF"
                                Text="Main Branch" />

                        </StackPanel>

                    </Border>

                    <!--  Menu  -->

                    <Button
                        Height="42"
                        HorizontalContentAlignment="Left"
                        Background="Transparent"
                        BorderThickness="0">

                        <StackPanel Orientation="Horizontal">

                            <materialDesign:PackIcon
                                Width="18"
                                Height="18"
                                Margin="12,0,10,0"
                                Kind="AccountCircleOutline" />

                            <TextBlock VerticalAlignment="Center" Text="My Profile" />

                        </StackPanel>

                    </Button>

                    <Button
                        Height="42"
                        HorizontalContentAlignment="Left"
                        Background="Transparent"
                        BorderThickness="0">

                        <StackPanel Orientation="Horizontal">

                            <materialDesign:PackIcon
                                Width="18"
                                Height="18"
                                Margin="12,0,10,0"
                                Kind="LockReset" />

                            <TextBlock VerticalAlignment="Center" Text="Change Password" />

                        </StackPanel>

                    </Button>

                    <Button
                        Height="42"
                        HorizontalContentAlignment="Left"
                        Background="Transparent"
                        BorderThickness="0">

                        <StackPanel Orientation="Horizontal">

                            <materialDesign:PackIcon
                                Width="18"
                                Height="18"
                                Margin="12,0,10,0"
                                Kind="CogOutline" />

                            <TextBlock VerticalAlignment="Center" Text="Settings" />

                        </StackPanel>

                    </Button>

                    <Separator Margin="10,4" />

                    <Button
                        x:Name="LogoutButton"
                        Height="42"
                        HorizontalContentAlignment="Left"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="LogoutButton_Click"
                        Foreground="#DC2626">

                        <StackPanel Orientation="Horizontal">

                            <materialDesign:PackIcon
                                Width="18"
                                Height="18"
                                Margin="12,0,10,0"
                                Kind="Logout" />

                            <TextBlock VerticalAlignment="Center" Text="Logout" />

                        </StackPanel>

                    </Button>

                </StackPanel>

            </Border>

        </Popup>

    </Grid>

</UserControl>