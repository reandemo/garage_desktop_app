<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================================= -->
    <!-- Default Garage Window -->
    <!-- ========================================================= -->

    <Style x:Key="GarageWindow"
           TargetType="Window">

        <Setter Property="Background"
                Value="{StaticResource WindowBackgroundBrush}" />

        <Setter Property="Foreground"
                Value="{StaticResource TextPrimaryBrush}" />

        <Setter Property="FontFamily"
                Value="Segoe UI" />

        <Setter Property="FontSize"
                Value="13" />

        <Setter Property="WindowStartupLocation"
                Value="CenterScreen" />

        <Setter Property="ResizeMode"
                Value="CanResize" />

        <Setter Property="MinWidth"
                Value="900" />

        <Setter Property="MinHeight"
                Value="600" />

    </Style>

    <!-- ========================================================= -->
    <!-- Child Window -->
    <!-- ========================================================= -->

    <Style x:Key="GarageChildWindow"
           TargetType="Window"
           BasedOn="{StaticResource GarageWindow}">

        <Setter Property="Width"
                Value="1000" />

        <Setter Property="Height"
                Value="700" />

    </Style>

    <!-- ========================================================= -->
    <!-- Dialog Window -->
    <!-- ========================================================= -->

    <Style x:Key="GarageDialogWindow"
           TargetType="Window"
           BasedOn="{StaticResource GarageWindow}">

        <Setter Property="ResizeMode"
                Value="NoResize" />

        <Setter Property="ShowInTaskbar"
                Value="False" />

        <Setter Property="SizeToContent"
                Value="WidthAndHeight" />

        <Setter Property="WindowStartupLocation"
                Value="CenterOwner" />

    </Style>

    <!-- ========================================================= -->
    <!-- Login Window -->
    <!-- ========================================================= -->

    <Style x:Key="GarageLoginWindow"
           TargetType="Window"
           BasedOn="{StaticResource GarageWindow}">

        <Setter Property="ResizeMode"
                Value="NoResize" />

        <Setter Property="Width"
                Value="500" />

        <Setter Property="Height"
                Value="650" />

    </Style>

    <!-- ========================================================= -->
    <!-- Splash Window -->
    <!-- ========================================================= -->

    <Style x:Key="GarageSplashWindow"
           TargetType="Window"
           BasedOn="{StaticResource GarageWindow}">

        <Setter Property="ResizeMode"
                Value="NoResize" />

        <Setter Property="WindowStyle"
                Value="None" />

        <Setter Property="AllowsTransparency"
                Value="True" />

        <Setter Property="Background"
                Value="Transparent" />

        <Setter Property="Width"
                Value="450" />

        <Setter Property="Height"
                Value="300" />

    </Style>

</ResourceDictionary>